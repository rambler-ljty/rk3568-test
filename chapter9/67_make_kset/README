# 67_make_kset - Linux内核kset创建示例模块

## 主要功能

### 1. kset创建和管理
- 使用`kset_create_and_add()`创建kset对象
- 将kset添加到sysfs文件系统中

### 2. kobject与kset关联
- 手动分配kobject内存空间
- 设置kobject的kset属性
- 将kobject添加到指定的kset中
- 在sysfs中形成层次化的目录结构
