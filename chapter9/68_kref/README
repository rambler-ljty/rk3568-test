# 68_kref - Linux内核kref引用计数示例模块

## 主要功能

### 1. kref引用计数演示
- 展示kobject中kref引用计数的工作原理
- 通过printk打印引用计数的变化过程
- 观察父子关系对引用计数的影响

### 2. 引用计数变化观察
- 创建kobject时的初始引用计数
- 建立父子关系时的引用计数变化
- 调用kobject_put()时的引用计数递减

### 3. 父子关系影响
- 子对象创建时父对象的引用计数增加
- 父对象释放时对子对象的影响
- 引用计数为0时的自动释放机制

