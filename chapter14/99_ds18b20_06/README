# DS18B20温度传感器Linux内核驱动模块 - 温度精度配置版本

## 概述
这是DS18B20温度传感器的Linux内核驱动模块的温度精度配置版本，
相比前七个版本增加了完整的温度精度配置功能，实现了从硬件通信到用户空间温度显示的完整流程，
并支持动态配置温度精度

## 主要功能特性

### 1. 平台驱动框架
- 实现标准的Linux平台驱动结构
- 支持设备树（Device Tree）匹配机制
- 使用`platform_driver`架构，便于设备管理

### 2. 字符设备驱动
- 创建字符设备节点，支持用户空间访问
- 实现基本的文件操作接口（open、read、release）
- 设备节点路径：`/dev/ds18b20`
- 设备类：`/sys/class/sensors/ds18b20`

### 3. GPIO集成控制
- 集成GPIO引脚管理
- 从设备树中获取名为"ds18b20"的GPIO引脚
- 自动配置GPIO为输出模式，初始值为高电平
- 符合DS18B20通信协议要求（数据线保持高电平状态）

### 4. DS18B20复位协议
- 实现DS18B20的复位时序控制
- 精确的微秒级延时控制
- 完整的1-Wire复位-存在脉冲检测
- 模块加载后自动执行复位操作

### 5. DS18B20写操作功能
- **写位操作**：实现单个位的写入，支持0和1的精确时序控制
- **写字节操作**：通过逐位写入实现完整字节的发送
- **时序控制**：精确的微秒级延时，符合1-Wire协议要求
- **命令发送**：支持发送各种控制命令到DS18B20

### 6. DS18B20读操作功能
- **读位操作**：实现单个位的读取，支持0和1的精确时序控制
- **读字节操作**：通过逐位读取实现完整字节的接收
- **时序控制**：精确的微秒级延时，符合1-Wire协议要求
- **数据接收**：支持从DS18B20读取各种数据

### 7. DS18B20温度读取应用功能
- **温度转换命令**：自动发送开始转换命令（0x44）
- **温度数据读取**：读取温度低字节和高字节
- **数据组合**：将两个字节组合成16位温度数据
- **用户空间接口**：通过read()函数向用户空间提供温度数据
- **数据安全传输**：使用copy_to_user确保内核空间到用户空间的安全传输

### 8. **新增：DS18B20温度精度配置功能**
- **精度设置命令**：通过写暂存器命令（0x4E）设置温度精度
- **精度选项支持**：支持9、10、11、12位精度设置
- **配置寄存器写入**：写入TH、TL和配置寄存器值
- **精度验证功能**：读取配置寄存器验证设置是否成功
- **动态精度调整**：运行时动态调整温度精度

### 9. **新增：ioctl控制接口**
- **设备控制命令**：实现unlocked_ioctl接口
- **精度设置命令**：SET_RESOLUTION命令支持
- **参数验证**：验证精度参数的有效性（9-12）
- **状态反馈**：返回设置成功或失败的状态

### 10. **增强：用户空间应用程序**
- **命令行参数支持**：支持精度参数输入（9-12）
- **参数验证**：验证参数范围的正确性
- **ioctl调用**：通过ioctl接口设置温度精度
- **实时温度显示**：连续读取并显示温度数据
- **用户友好**：显示正负号和精确到小数点后4位的温度值

### 11. 设备树支持
- 通过`compatible="ds18b20"`属性匹配设备

### 12. 完整的资源管理
- 动态分配设备号
- 创建字符设备、设备类、设备节点
- GPIO资源获取和配置
- 完善的错误处理和资源清理机制
- 支持模块动态加载/卸载


## 版本对比分析

| 功能特性 | 94_01版本 | 94_02版本 | 95_01版本 | 95_02版本 | 96_03版本 | 97_04版本 | 98_05版本 | 99_06版本 |
|---------|-----------|-----------|-----------|-----------|-----------|-----------|-----------|-----------|
| 平台驱动框架 | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| 字符设备驱动 | ❌ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| 用户空间接口 | ❌ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| GPIO集成控制 | ❌ | ❌ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| 复位协议 | ❌ | ❌ | ❌ | ✅ | ✅ | ✅ | ✅ | ✅ |
| 写操作功能 | ❌ | ❌ | ❌ | ❌ | ✅ | ✅ | ✅ | ✅ |
| 读操作功能 | ❌ | ❌ | ❌ | ❌ | ❌ | ✅ | ✅ | ✅ |
| 温度读取应用 | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ | ✅ | ✅ |
| 用户空间程序 | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ | ✅ | ✅ |
| **温度精度配置** | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ | ✅ |
| **ioctl控制接口** | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ | ✅ |
| 资源管理 | 基础 | 完整 | 完整+GPIO | 完整+GPIO+协议 | 完整+GPIO+协议+写操作 | 完整+GPIO+协议+写操作+读操作 | 完整+GPIO+协议+写操作+读操作+温度应用 | 完整+GPIO+协议+写操作+读操作+温度应用+精度配置 |


