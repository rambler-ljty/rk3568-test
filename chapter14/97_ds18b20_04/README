# DS18B20温度传感器Linux内核驱动模块 - 读写完整版本

## 概述
这是DS18B20温度传感器的Linux内核驱动模块的读写完整版本，
相比前五个版本增加了DS18B20读位和读字节操作功能，实现了1-Wire通信协议的完整读写操作

单总线通信协议

## 主要功能特性

### 1. 平台驱动框架
- 实现标准的Linux平台驱动结构
- 支持设备树（Device Tree）匹配机制
- 使用`platform_driver`架构，便于设备管理

### 2. 字符设备驱动
- 创建字符设备节点，支持用户空间访问
- 实现基本的文件操作接口（open、read、release）
- 设备节点路径：`/dev/ds18b20`
- 设备类：`/sys/class/sensors/ds18b20`

### 3. GPIO集成控制
- 集成GPIO引脚管理
- 从设备树中获取名为"ds18b20"的GPIO引脚
- 自动配置GPIO为输出模式，初始值为高电平
- 符合DS18B20通信协议要求（数据线保持高电平状态）

### 4. DS18B20复位协议
- 实现DS18B20的复位时序控制
- 精确的微秒级延时控制
- 完整的1-Wire复位-存在脉冲检测
- 模块加载后自动执行复位操作

### 5. DS18B20写操作功能
- **写位操作**：实现单个位的写入，支持0和1的精确时序控制
- **写字节操作**：通过逐位写入实现完整字节的发送
- **时序控制**：精确的微秒级延时，符合1-Wire协议要求
- **命令发送**：支持发送各种控制命令到DS18B20

### 6. **新增：DS18B20读操作功能**
- **读位操作**：实现单个位的读取，支持0和1的精确时序控制
- **读字节操作**：通过逐位读取实现完整字节的接收
- **时序控制**：精确的微秒级延时，符合1-Wire协议要求
- **数据接收**：支持从DS18B20读取各种数据

### 7. 设备树支持
- 通过`compatible="ds18b20"`属性匹配设备

### 8. 完整的资源管理
- 动态分配设备号
- 创建字符设备、设备类、设备节点
- GPIO资源获取和配置
- 完善的错误处理和资源清理机制
- 支持模块动态加载/卸载


### 1. 设备树配置
在设备树中添加GPIO配置：
```dts
ds18b20 {
    compatible = "ds18b20";
    ds18b20-gpios = <&gpio0 18 GPIO_ACTIVE_HIGH>;  // 示例GPIO配置
};
```

## 版本对比分析

| 功能特性 | 94_01版本 | 94_02版本 | 95_01版本 | 95_02版本 | 96_03版本 | 97_04版本 |
|---------|-----------|-----------|-----------|-----------|-----------|-----------|
| 平台驱动框架 | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| 字符设备驱动 | ❌ | ✅ | ✅ | ✅ | ✅ | ✅ |
| 用户空间接口 | ❌ | ✅ | ✅ | ✅ | ✅ | ✅ |
| GPIO集成控制 | ❌ | ❌ | ✅ | ✅ | ✅ | ✅ |
| 复位协议 | ❌ | ❌ | ❌ | ✅ | ✅ | ✅ |
| 写操作功能 | ❌ | ❌ | ❌ | ❌ | ✅ | ✅ |
| **读操作功能** | ❌ | ❌ | ❌ | ❌ | ❌ | ✅ |
| 资源管理 | 基础 | 完整 | 完整+GPIO | 完整+GPIO+协议 | 完整+GPIO+协议+写操作 | 完整+GPIO+协议+写操作+读操作 |



## 1-Wire协议特点
1-Wire协议是一种单总线通信协议，具有以下特点：
- **单根数据线**：只需要一根数据线即可实现双向通信
- **时序严格**：对时序要求非常严格，需要微秒级精度控制
- **双向通信**：同一根数据线既可以发送数据也可以接收数据
- **协议简单**：通信协议相对简单，但时序控制复杂
- **应用广泛**：广泛应用于温度传感器、EEPROM等设备
