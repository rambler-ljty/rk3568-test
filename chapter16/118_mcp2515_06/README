# MCP2515 CAN 控制器 SPI 驱动（CAN 通信版本）

## 概述

实现了 MCP2515 CAN 控制器的 Linux 内核 SPI 驱动模块，并提供了字符设备接口。
用户空间程序可以通过标准的文件操作接口（open、read、write、close）与 MCP2515 进行通信。

增加 SPI 通信功能，包括芯片复位、寄存器读取、写入、位操作以及完整的 CAN 控制器配置和初始化。
实现了完整的 CAN 消息发送和接收功能。


