# myinput_dev - Linux内核输入设备驱动模块

## 项目概述
这是一个简单的Linux内核输入设备驱动模块，用于演示Linux输入子系统的基本用法。

### 核心功能
- **虚拟输入设备创建**：创建一个名为"myinput_dev"的虚拟输入设备
- **按键事件支持**：支持按键事件类型（EV_KEY）
- **特定按键支持**：支持数字键1（KEY_1）的输入事件

