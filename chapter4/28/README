实现了一个支持ioctl操作的字符设备驱动程序，具体功能如下：
1.设备驱动功能：
  实现了一个基本的字符设备驱动
  支持通过ioctl命令与用户空间进行通信
  可以接收用户空间传递的结构体参数
2.主要组件：
  ioctl.c：内核模块代码，实现了设备驱动的核心功能
  ioctl：用户空间测试程序，用于测试设备驱动的功能
3.具体实现细节：
  定义了一个参数结构体 args，包含三个整型参数（a、b、c）
  实现了ioctl命令 CMD_TEST0，用于接收用户空间传递的结构体数据
  当用户空间通过ioctl发送数据时，驱动会打印出接收到的三个参数值
4.设备操作流程：
  用户空间程序打开设备文件 /dev/test
  通过ioctl命令发送包含三个参数的结构体数据
  内核驱动接收数据并打印参数值
