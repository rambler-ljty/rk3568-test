1.定时器的定义和初始化：
  使用 DEFINE_TIMER 宏定义定时器结构体
  在模块初始化时设置定时器到期时间
  使用 add_timer 将定时器添加到系统
2.定时器回调函数：
  实现定时器触发时的回调函数
  在回调函数中重新设置定时器，实现周期性触发
  使用 mod_timer 修改定时器到期时间
3.模块的清理：
  在模块退出时删除定时器
  使用 del_timer 确保定时器被正确清理

这个示例展示了内核定时器的基本使用方法，包括：
1.定时器的创建和初始化
2.定时器回调函数的实现
3.定时器的周期性触发
4.定时器的清理和删除
