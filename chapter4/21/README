1.内核模块部分 (module/wq.c)：
  增加了非阻塞读操作的支持
  在 read 函数中增加了对 O_NONBLOCK 标志的检查
  当设备没有数据时，非阻塞模式下会返回 -EAGAIN
2.测试应用程序部分：
  read.c：
    使用 O_NONBLOCK 标志打开设备
    实现了循环读取功能
    每秒读取一次数据
  write.c：
    同样使用 O_NONBLOCK 标志打开设备
    写入数据 "nihao"

这个版本的程序主要演示了：
1.设备驱动的非阻塞操作
2.循环读取设备数据的方法
3.非阻塞模式下的错误处理
